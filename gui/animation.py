import PySimpleGUI as sg
import itertools
import threading
import time
import sys

def add_animation(function,animation) :
    def inner() :




def _default_loading() :
    """
    Use a Thread to target this function, and set global parameter end_loading to True to finish.
    """
    while True :
        if end_loading :
            break